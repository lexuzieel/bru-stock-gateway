<?php

namespace App;

use DI\Container;

return function (Container $container) {
    // Services are resolved automatically through the controller.
    // You can add manual service definitions if needed.
};
